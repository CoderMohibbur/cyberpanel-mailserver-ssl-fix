# GitHub Publishing Guide

## Recommended repository name
`cyberpanel-mailserver-ssl-fix`

## Recommended description
Fix CyberPanel MailServer SSL issues where Gmail/Outlook reject SMTP TLS/SSL because Postfix/Dovecot keep serving an old or expired certificate.

## Recommended topics
- cyberpanel
- mailserver-ssl
- smtp
- smtp-tls
- postfix
- dovecot
- letsencrypt
- gmail-smtp
- ssl-certificate
- mail-server

## Suggested git commands
```bash
git status
git add .
git commit -m "Initial release: CyberPanel MailServer SSL fix"
git branch -M main
git remote -v
git push -u origin main
```
